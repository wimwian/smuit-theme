---
'@smuit/smuit-theme': minor
---

Typography updates: increase the `body` (xs–xl) and `label` (sm–lg) type-scale font sizes, and adjust the `display-md` line-height. The generated theme stylesheet now emits the standard `font-stretch` property in place of the previous non-standard `font-width`.
